-- maxx-mellow — thin alias over oldworld.nvim's default (dark) palette, so the
-- dark colorscheme is named to match its light companion, maxx-mellow-dawn,
-- and the ghostty theme of the same name.
require("oldworld").colorscheme()
vim.g.colors_name = "maxx-mellow"
